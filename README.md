

how to run the server locally

1. open the server folder
2. run `swift run App serve -H 0.0.0.0 -p 8081`
3. define the address of the server in `getOn/Networking/APIManager.swift` from the wifi address of the server on the local network (see wifi settings)
